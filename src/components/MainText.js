import styled from 'styled-components';

export default styled.h3`
  font-size: 1.5rem;
  font-weight: normal;
  margin-bottom: 48px;
  margin-top: 0;

  @media (min-width: 768px) {
    font-size: 2.5rem;
  }
`;
